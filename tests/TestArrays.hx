package ;

import tink.state.*;

using tink.CoreApi;

class TestArrays extends TestBase {
  @:describe("basic test")
  public function testEntries() {
    var a = new ObservableArray();
    done();
  }
}